name: 'Test Install Action'

on: [push]

jobs:
  install-action-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Install Action
        uses: ./ # Uses the action in the root directory of your repository
        with:
          package-name: 'node'
